<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.studyapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudyApp">
        <activity android:name=".CatActivity"></activity>
        <activity android:name=".CatsCategoryActivity" />
        <activity android:name=".TrainingOfPetsActivity" />
        <activity android:name=".ConstraintLayoutActivity" />
        <activity
            android:name=".PictureAndTextActivity"
            android:label="@string/title_activity_picture_and_text"
            android:theme="@style/Theme.StudyApp.NoActionBar" />
        <activity android:name=".StopwatchActivity" />
        <activity android:name=".ShowMessageActivity" />
        <activity android:name=".CreateMessageActivity" />
        <activity android:name=".ChooserActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>